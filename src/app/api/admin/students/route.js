import { requireAdmin } from '@/lib/auth';import { db } from '@/lib/db';export async function GET(){try{await requireAdmin();const rows=(await db().execute("SELECT u.id,u.full_name,u.ic_number,u.batch_no,u.email, COUNT(DISTINCT pi.project_id) as completed FROM users u LEFT JOIN project_images pi ON pi.user_id=u.id WHERE u.role='student' GROUP BY u.id ORDER BY u.created_at DESC")).rows;return Response.json({students:rows})}catch{return Response.json({error:'Forbidden'},{status:403})}}
