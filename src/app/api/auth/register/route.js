import { db } from '@/lib/db';import bcrypt from 'bcryptjs';import { cookies } from 'next/headers';import { createSession } from '@/lib/auth';
export async function POST(req){try{const b=await req.json();const hash=await bcrypt.hash(b.password,10);const r=await db.execute({sql:'INSERT INTO users (full_name,ic_number,batch_no,email,password_hash,role) VALUES (?,?,?,?,?,?) RETURNING *',args:[b.fullName,b.icNumber,b.batchNo,b.email.toLowerCase(),hash,'student']});const user=r.rows[0];(await cookies()).set('session',await createSession(user),{httpOnly:true,secure:true,sameSite:'lax',path:'/'});return Response.json({ok:true,role:user.role})}catch(e){return Response.json({error:'Email atau IC sudah digunakan.'},{status:400})}}
