"use client";import{useState}from'react';import Image from'next/image';
export default function Home(){const[mode,setMode]=useState('login'),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget).entries());const res=await fetch('/api/auth/'+mode,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await res.json();if(res.ok)location.href=data.role==='admin'?'/admin':'/dashboard';else setMsg(data.error||'Ralat berlaku')}return <main className="shell"><div className="brand"><Image className="logo" src="/logo.png" alt="Logo" width={86} height={86}/><div><h1>Portfolio Gambar Projek Amali SKM3 Kimpalan</h1><p>Sistem penghantaran gambar projek pelajar KAMS</p></div></div><div className="card"><div className="tabs"><button className={'tab '+(mode==='login'?'active':'')} onClick={()=>setMode('login')}>Log Masuk</button><button className={'tab '+(mode==='register'?'active':'')} onClick={()=>setMode('register')}>Daftar Pelajar</button></div><form onSubmit={submit} className="grid">{mode==='register'&&<><input className="input" name="fullName" placeholder="Nama penuh" required/><input className="input" name="icNumber" placeholder="No kad pengenalan" required/><input className="input" name="batchNo" placeholder="No batch" required/></>}<input className="input" name="email" type="email" placeholder="Email" required/><input className="input" name="password" type="password" placeholder="Password" required/><button className="btn">{mode==='login'?'Log Masuk':'Daftar Akaun'}</button>{msg&&<p className="badge red">{msg}</p>}<p className="small">Admin log masuk guna ADMIN_EMAIL dan ADMIN_PASSWORD.</p></form></div></main>}
